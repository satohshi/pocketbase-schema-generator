{
	"name": "hook",
	"version": "0.7.1",
	"author": "satohshi",
	"license": "MIT",
	"private": true,
	"scripts": {
		"dev": "tsc -w -p tsconfig.build.json --outDir ../../pb/pb_hooks/",
		"dev:parallel": "tsc -w -p tsconfig.build.json --outDir ../../pb/pb_hooks/",
		"build": "tsc -p tsconfig.build.json --outDir ../../pb_hooks/",
		"lint": "tsc --noEmit",
		"lint:watch": "tsc -w --noEmit",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"ajv": "^8.17.1",
		"typescript": "^5.7.3",
		"vitest": "^3.0.5"
	}
}
